# Usage
First, activate the edf by using 'source ~/.espressif/tools/activate_idf_v6.0.sh'
Next, build and flash using 'idf.py build && idf.py flash'
FInally, run 'python capture.py'

# Setup
You need to change the server address before using
Also, you need esptouch app to setup wifi
